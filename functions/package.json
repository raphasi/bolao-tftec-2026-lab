{
  "name": "@fifa2026-bolao/functions",
  "version": "0.1.0",
  "private": true,
  "description": "Azure Functions do bolão — scoring + leaderboard agregation (Sprint S3)",
  "type": "module",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "start": "func start",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@azure/cosmos": "^4.2.0",
    "@azure/functions": "^4.5.1",
    "p-limit": "^7.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
