# Em dev, deixe vazio ou comente — Vite proxia /api -> backend:3001 (vite.config.ts)
# Em build de produção, o backend serve o frontend estático na mesma origem,
# então /api funciona sem configuração.
# VITE_API_BASE_URL=https://app-fifa-bolao-XXXX.azurewebsites.net/api
